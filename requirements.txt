В данном проекте используются библиотеки:
os, sys, requests, flask (Flask, render_template, request, redirect, url_for, flash), flask_sqlalchemy (SQLAlchemy), flask_bcrypt (Bcrypt), flask_login (LoginManager, UserMixin, login_user, current_user, 
logout_user, login_required), werkzeug.utils (secure_filename), product_data (products_list)
